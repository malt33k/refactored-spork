import XCTest
@testable import refactored_spork

final class refactored_sporkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(refactored_spork().text, "Hello, World!")
    }
}
